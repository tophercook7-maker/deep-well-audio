Place your sitewide background source image here with this exact filename:

  a_photograph_captures_a_pair_of_adul.png

Then run:

  npm run build:atmosphere

That generates optimized public/atmosphere/site-atmosphere.webp (and updates site-cinematic-bg.webp for compatibility).
Prebuild runs build:atmosphere automatically on `npm run build`.
